server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # This makes React Router-style client-side routing work correctly:
    # any path that doesn't match a real file falls back to index.html,
    # letting React handle the routing itself.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
